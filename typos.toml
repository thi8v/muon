# Configuration file of typos
#
# See docs: https://github.com/crate-ci/typos/blob/v1.28.2/docs/reference.md

[default.extend-words]
# we can't use 'type' in Rust because it's a keyword so we use 'typ'.
typ = "typ"
