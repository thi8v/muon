# `E0005` -- empty character literal

```muon
let c = '';
```

In this erroneous example the character literal is empty but a character literal
must have excatly one codepoint between its single quotes.
