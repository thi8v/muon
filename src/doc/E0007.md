# `E0007` -- not enough hexadecimal digits

```muon
let c = '\xC';
let s = "Hello \xA"
```

In the example, there is not enough hexadecimal digits in an escape sequence,
consider adding a zero before.
