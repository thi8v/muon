# Error `E0004`

```muon
let a = 'ABC';
```

In this erroneous example, there is 3 characters inside of a character literal,
you should consider using a string literal instead like that: `"ABC"`, if you
want multiple characters in a row.
