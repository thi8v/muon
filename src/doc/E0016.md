# Error `E0016`

The provided Abi in the string literal of an extern block item doesn't exist.

Erroneous example:
```muon
extern "HELLO" {}
```

To fix the error you simply choose a valid calling convention. Example:
```muon
extern "C" {}

extern "Muon" {}
```
